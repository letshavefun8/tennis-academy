---
name: deploy-frontend
description: Деплой фронтенда «Академии тенниса» на GitHub Pages — бамп версии ассетов (?v=N) и APP_VERSION, коммит и push. Используй, когда просят задеплоить/выкатить фронт, опубликовать сборку, обновить прод.
---

Процедура выката фронтенда на GitHub Pages. Делай строго по шагам.

1. Текущий номер: `grep -n "?v=" index.html` (все 4 ссылки с одним N) и `APP_VERSION` в app.js (~строка 9). Новый номер = N+1.
2. Забампь синхронно:
   - index.html: `?v=N` → `?v=N+1` у style.css, cloud.js, questions.js, app.js (4 ссылки);
   - app.js: `APP_VERSION = 'сборка N+1 · ДД.ММ'` (дата — сегодня).
3. Если правил app.js — `node --check app.js`.
4. Покажи `git diff` — убедись, что изменения только версионные.
5. `git add` изменённых файлов → `git commit -m "..."` → `git push origin main`.
   GitHub Pages пересоберёт сайт из main сам. Команда `git push` намеренно требует подтверждения — это последняя проверка перед выкатом в прод.
6. Отчёт: новый номер сборки, что закоммичено/запушено.

Важно: коммить/пушь только то, что относится к выкату. Посторонние изменения в дереве — спроси, прежде чем включать.
